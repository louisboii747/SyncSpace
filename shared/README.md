# Shared contracts

SyncSpace shares protocol and behavior contracts rather than cross-platform UI
widgets. Native clients consume the same REST models and WebSocket events while
keeping platform-appropriate interaction and accessibility.

See [the transfer UI contract](../docs/transfer-ui.md), [API](../docs/api.md),
and [file transfer protocol](../docs/transfer-protocol.md).
